services:
  app:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      # Данные лежат в томе, а не внутри контейнера.
      DATABASE_URL: sqlite+aiosqlite:////data/family.db
    ports:
      - "8080:8080"
    volumes:
      - family-data:/data

volumes:
  family-data:
